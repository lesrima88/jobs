 <br>
      <br>
      <br>
      <br>
      <br>
      <br>
      <br>

<div class="container">
	<div class="row">
      <div class="col-md-4 col-md-offset-4">
      <center>
		   <div class="panel panel-default">
             <div class="panel-body">
               <center>




       <% if @request.completed? %>
      
     

      <h1> This request was completed  </h1>

       <% else %>

      <%if @request.user.provider=='facebook' %>
        <p>Requested by <%= link_to(@request.user.name, @request.user) %></p>
        <%= link_to (image_tag @request.user.image, :class=> "img-circular"), @request.user %>
        <% else %>
          <p>Requested by <%= link_to(@request.user.first_name, @request.user) %></p>
          <%= link_to (image_tag @request.user.avatar, :class=> "img-circular"), @request.user %>

          <% end %>

	
	    
	

	       <p>Member since<%= @request.user.created_at.strftime(" %b %d, %Y") %></p>
	      <p><%= link_to("View Profile", @request.user) %><p>
      </center>
	 
     </div>
       </div>
         </div>

     							

     							<div class="container">
         				<div class="row">
        



              <div class="col-md-4 col-md-offset-4">
       <div class="panel panel-default">
         
         <div class="panel-body">
         <center><h3><b><%= @request.title %></b></h3></center>

			
			<center><p><%= @request.body    %></p></center>
    
		 







<%-# link_to("Seller's Profile", @request.user) %>




<% end %>






<%#= link_to :recipient_id => @request.user.id(type: "submit", class: "btn btn-primary") do %>


 




  <p align="center"><p></p>

    










<p style="text-align:left;">
&nbsp;<span class="glyphicon glyphicon-eye-open glyphicon-align-left" aria-hidden="true"></span>&nbsp;<%=  @request.city.try(:name)    %>
<span style="float:right;"><%= @request.created_at.strftime(" %b %d, %Y") %></span>
</p>
</div>
</div>
</div>
</div>
</div>


<% if user_signed_in? %>

<% if @request.user_id == current_user.id %>

 <div class="col-md-4 col-md-offset-4">
       <div class="panel panel-default">
         
         <div class="panel-body">
    
   
<center><%= link_to complete_request_path(@request), method: :patch do %></p>
<i class="fa fa-check fa-lg"></i><% end %><%= link_to "Edit", edit_request_path(@request) ,class: "btn btn-md btn-primary"  %>
<%= link_to "Delete", request_path(@request), method: :delete, data: { confirm: "Are you sure to permanently delete this listed request?" }, class: "btn btn-md btn-danger" %></center>

         </div>
         </div>
         </div>
  <% end %>
  <% end %>


 <% if @request.completed? %>

<% else %>

 <div class="col-md-4 col-md-offset-4">
		   <div class="panel panel-default">
         
         <div class="panel-body">
         <center><h3><b>Payout</b></h3></center>
         <%if @request.user.provider=='facebook' %>

         <p> <%= @request.user.name %> is offering Ksh <%= number_with_delimiter(@request.budget, :delimiter => ',') %>/- if you complete this request</p>
         <% else %>
   <p> <%= @request.user.first_name %> is offering Ksh <%= number_with_delimiter(@request.budget, :delimiter => ',') %>/- if you complete this request</p>
         </div>
         </div>
         </div>
         <% end %>



        


 <div class="col-md-4 col-md-offset-4">
		   <div class="panel panel-default">
         
         <div class="panel-body">
         <center>
         
     
     <p>Can you fullfill this request?<p>


   <div class="ui primary circular segment">
  <h2 class="ui header">
    Contact
    <div class="sub header"><%= @request.contact %></div>
  </h2>
</div>
   <% end %>




   
    <% if user_signed_in? %>
   
	<%if @request.user_id == current_user.id %>

  <%if @request.completed? %>

	<% else %>



   <center><%= link_to "Send Message", new_conversation_path(:recipient_id => @request.user.id), class: "btn btn-large btn-primary"%></center>
    
	<%#= link_to "Message Seller", new_conversation_path(:recipient_id => @request.user.id), class: "btn btn-large btn-primary" %>
	<% end %>
	<% end %>
  <% end %>
	</center>
	</center>
    </div>
    </div>
    </div>




